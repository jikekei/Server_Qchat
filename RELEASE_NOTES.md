# v1.2.0 — 游戏内推送 & 通信鉴权

## 🚀 新功能

### 游戏内 `.ac` 实时推送指令
玩家在游戏中按 `~` 打开控制台，输入 `.ac <消息>`，即可将消息**实时推送至指定 QQ 群**。
- 全异步非阻塞，不影响游戏帧率
- 推送目标群号统一在机器人端配置，多服管理更方便
- 发送时，当前对局所有在线管理员会同步收到屏幕广播提示

### 🔒 双向通信鉴权 (AuthToken)
为所有 TCP 通信增加了预共享密钥校验：
- 机器人 → 游戏服：群指令（查人数、踢人、广播等）带 Token 验证
- 游戏服 → 机器人：`.ac` 推送带 Token 验证，防止第三方伪造消息
- Token 留空则自动回退为无鉴权模式，兼容旧版

### 后台通知监听服务
机器人端新增独立 TCP 监听端口（默认 `10088`），专门接收游戏服推送，与命令端口 `10087` 互不影响。支持自定义监听 IP。

---

## ⚙️ 新增配置项

### 游戏服端 (EXILED `config.yml`)
```yaml
bot_i_p: '127.0.0.1'        # 机器人通知监听 IP
bot_port: 10088              # 机器人通知监听端口
auth_token: 'YourToken'      # 鉴权密钥，须与机器人端一致
```

### 机器人端 (`appsettings.json`)
```json
"SocketServer": {
  "NotificationHost": "0.0.0.0",
  "NotificationPort": 10088,
  "AuthToken": "YourToken"
},
"Bot": {
  "AcTargetGroupId": 123456789
}
```

---

## ⚠️ 升级须知

1. **必须修改默认 Token**：默认值 `QchaSecret_123` 仅供演示，请替换为自定义强密码
2. **双端 Token 必须一致**：否则所有通信将被拦截并返回 `Unauthorized`
3. **配置目标群号**：在机器人端 `Bot:AcTargetGroupId` 中填入接收推送的 QQ 群号
4. 替换 `Server_Qcha.dll` 后需重启游戏服务器，机器人端同理

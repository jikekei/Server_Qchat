# v1.3.0 — 服务器自动注册 & 心跳机制

## 🚀 新功能

### 🔌 服务器自动端口注册
游戏服插件启动时**自动向机器人端注册**，机器人端**无需再手动配置 `Ports` 数组**。

- **原子端口探测**：插件从配置的 `tcp_port` 起始，自动尝试绑定可用端口（最多探测 100 个），绝不再有"先探测后绑定"的竞争窗口
- **动态注册表**：机器人端新增 `ServerRegistry` 动态服务注册表，使用 `{host}:{port}` 复合键管理所有在线服务器，支持多主机场景互不冲突
- **零配置多服部署**：多个游戏服使用完全相同的默认配置即可启动，端口自动探测、注册自动完成

### 💓 心跳保活 & 指数退避
插件每 30 秒向机器人发送心跳包（携带完整注册信息），确保持续在线。

- **自动恢复**：机器人重启后，下一次心跳（≤30 秒）自动重新注册，无需人工干预
- **指数退避**：心跳失败时自动退避（30s → 60s → 120s → 240s → 300s 封顶），避免网络中断时的发包风暴
- **慢速模式**：连续 10 次失败后降频为每 5 分钟一次，网络恢复后立即回到正常频率
- **心跳超时清理**：机器人端每 60 秒扫描一次，90 秒无心跳的动态服务器自动移除

### 🛡️ 三层离线感知
| 场景 | 感知方式 | 延迟 |
|------|----------|------|
| 正常关闭 | 插件发送 `unregister` 注销包 | **0 秒** |
| 崩溃 + 有人用指令 | 命令下发失败即时标记离线 | **< 1 秒** |
| 崩溃 + 无人用指令 | 心跳超时自动清理 | **≤ 90 秒** |

### 🔢 多服排序机制
新增 `sort_order` 配置项，灵活控制 `#1` `#2` `#3` 的对应关系。

- **手动排序**：设置 `sort_order: 1`、`sort_order: 2` 指定服务器编号
- **自动排序**：`sort_order: 0`（默认）时按游戏运行端口自动排序
- **混合排序**：手动优先排列，未配置的自动排在后面
- **完全确定**：多级排序键（SortOrder → GamePort → ConnectHost → Port）确保排序结果稳定不漂移

### 📋 统一 JSON 信封协议
通知通道全面升级为 JSON 信封格式，彻底解决旧版文本前缀与 `.ac` 用户输入混淆的安全隐患。

```
{AuthToken}||{"type":"register|unregister|heartbeat|ac","data":{...}}
```
- 所有消息均为**请求-响应**模式（机器人回复 `"OK"` 确认）
- 用户输入内容封装在 `data.message` 中，无论内容是什么都不会影响类型判断

### 📝 全面日志规范
双端新增了覆盖全生命周期的结构化日志，方便快速定位多服环境下的问题。

---

## ⚙️ 新增配置项

### 游戏服端 (EXILED `config.yml`)

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `sort_order` | int | `0` | 排序权重，>0 按此排序，=0 自动排序 |
| `connect_host` | string | `""` | 机器人回连本服务器的 IP。同机留空，跨机填局域网/公网 IP |

### 机器人端 (`appsettings.json`)

| 配置项 | 变更 | 说明 |
|--------|------|------|
| `SocketServer:Ports` | **改为可选** | 留空 `[]` 即可，由游戏服自动注册 |

---

## 📦 涉及文件变更

### 游戏服端 (server/)
| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `Main.cs` | 修改 | 新增 `SortOrder`、`ConnectHost` 配置 + 原子端口探测 + 注册/注销/心跳循环 |
| `BotNotificationClient.cs` | 修改 | JSON 信封协议 + TCP 半关闭等待响应 + 退避重试 |
| `TcpCommandServer.cs` | 修改 | 命令处理审计日志 |
| `AcCommand.cs` | 修改 | `.ac` 推送改为 JSON 信封格式 |

### 机器人端 (bot/)
| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `Socket/ServerRegistry.cs` | **新增** | 线程安全注册表 + 复合键 + 多级排序 + 超时清理 + MarkOffline |
| `Socket/BotNotificationListenerService.cs` | 修改 | JSON 信封解析 + 四种 type 路由 + 心跳重注册 + 响应回写 |
| `Socket/SocketCommandClient.cs` | 修改 | `SendAsync` 支持动态 host+port |
| `Bot/CommandRouter.cs` | 修改 | 切换为 `ServerRegistry` + 命令失败即时标记离线 |
| `Program.cs` | 修改 | 注册 `ServerRegistry` 单例 |

---

## ⬆️ 升级指南

### 多服全自动部署（推荐）
```yaml
# 所有游戏服使用完全相同的默认配置
tcp_port: 10087        # 全部默认，自动探测可用端口
sort_order: 0          # 自动按游戏端口排序
connect_host: ''       # 同机部署留空

# 机器人端
"Ports": []            # 留空，无需手动填写
```

### 多服手动排序
```yaml
# 游戏服 A：群里排第 2
sort_order: 2

# 游戏服 B：群里排第 1
sort_order: 1

# 游戏服 C：自动排最后
sort_order: 0
```

### 升级步骤
1. 替换游戏服端 `Server_Qcha.dll`（新版本会自动在配置中生成 `sort_order` 和 `connect_host` 字段）
2. 替换机器人端程序
3. 可选：清空机器人端 `SocketServer:Ports` 数组以启用全自动注册（保留旧配置也兼容）
4. 重启双端程序
5. 观察日志确认注册成功：
   - 游戏服端：`[Server_Qcha] 注册成功，机器人已确认`
   - 机器人端：`服务器注册 → [1服] 127.0.0.1:10087 (gamePort=7777, sortOrder=0)`

---

## ⚠️ 注意事项

- **向下兼容**：手动配置的 `Ports` 条目仍然有效（标记为静态，豁免心跳超时清理）
- **跨机部署**：需在游戏服端手动配置 `connect_host` 为本机可路由的 IP 地址
- **默认 Token**：请务必修改默认的 `QchaSecret_123`，并确保双端 Token 一致

# Server_Qcha 项目交接文档

> 编写日期：2026-09-20
> 代码状态：`server/` 插件 v1.3.0（开发中）、`bot/` 机器人含 Web 管理面板（未发布）
> 适用读者：接手本项目的开发者 / 运维

---

## 目录

1. [项目是什么](#一项目是什么)
2. [系统组成与架构](#二系统组成与架构)
3. [目录结构](#三目录结构)
4. [核心模块说明](#四核心模块说明)
5. [通信协议](#五通信协议)
6. [配置说明](#六配置说明)
7. [部署与运行](#七部署与运行)
8. [Web 面板权限模型](#八web-面板权限模型)
9. [完成度与 TODO](#九完成度与-todo)
10. [已知问题与技术债](#十已知问题与技术债)
11. [构建与开发](#十一构建与开发)
12. [后续演进建议](#十二后续演进建议)

---

## 一、项目是什么

把 **QQ 群** 和 **SCP: Secret Laboratory（SCPSL）专用服务器** 打通，并提供 Web 管理面板。

| 方向 | 能力 |
|------|------|
| QQ 群 → 游戏服 | 群内发指令查询在线、广播、踢人封禁、重启回合 |
| 游戏服 → QQ 群 | 玩家游戏内 `.ac <消息>` 实时推送到群；服务器上下线；心跳保活 |
| 浏览器 → 游戏服 | Web 面板做同样的管理操作，带账号权限体系与审计日志 |

所有 TCP 通信带 `AuthToken` 双向鉴权。

---

## 二、系统组成与架构

三个可独立部署的部分，其中 **Web 面板与 QQ 机器人共用同一个进程**：

| 组件 | 代码位置 | 技术栈 | 运行在 |
|------|---------|--------|--------|
| 游戏服插件（Server） | `server/` | C# / .NET Framework **4.8.1** / EXILED 9.5.0 | SCPSL 服务端进程内（Unity Mono） |
| QQ 机器人（Bot） | `bot/src/Server_Qcha.Bot` | C# / **.NET 8** | 独立进程 |
| Web 管理面板 | 同上（同进程） | ASP.NET Core 8 + Vue 3 | 随机器人启动 |

```
                    ┌──────────────────────────────────────────┐
   浏览器 ──HTTP──→ │  Bot 进程（.NET 8）                       │
   (8080)           │  ├─ Kestrel  → Web 面板 API + 静态页面    │
                    │  ├─ GoCqHttpBotService → QQ 群消息处理    │
                    │  ├─ BotNotificationListenerService :10088 │
                    │  └─ ServerRegistry（服务器拓扑）          │
                    └───────┬──────────────────────▲───────────┘
                            │                      │
              命令通道 TCP  │ 10087                │ 通知通道 TCP 10088
              (Token||命令) │                      │ (Token||JSON)
                            ▼                      │
                    ┌──────────────────────────────────────────┐
                    │  游戏服进程（SCPSL + EXILED，net481）      │
                    │  ├─ TcpCommandServer :10087  收指令       │
                    │  ├─ CommandDispatcher        执行指令     │
                    │  ├─ AcCommand                游戏内 .ac   │
                    │  └─ BotNotificationClient    注册/心跳/推送│
                    └──────────────────────────────────────────┘

   QQ 群 ←── OneBot 11 正向 WebSocket (ws://127.0.0.1:6700) ── NapCatQQ
```

**三个关键设计**

1. **游戏服插件主动外连**：注册与心跳均由插件发起，机器人不主动连游戏服。好处是游戏服无需开放入站端口（NAT/内网友好），多服零配置。
2. **两条独立 TCP 通道**：`10087` 是「机器人 → 游戏服」请求-响应；`10088` 是「游戏服 → 机器人」推送。
3. **Web 面板复用同一条命令通道**：面板指令与 QQ 群指令走完全相同的链路（`SocketCommandClient`），不存在第二套逻辑。

---

## 三、目录结构

```
SocketServer-SCPSL-socks/
├── server/                      # SCPSL 游戏服插件（EXILED，net481）
│   ├── Main.cs                  # 插件入口、Config、启动 TCP 服务 + 心跳循环
│   ├── TcpCommandServer.cs      # TCP 命令监听（默认 10087，占用则自动递增）
│   ├── CommandDispatcher.cs     # 指令分发与游戏内操作
│   ├── AcCommand.cs             # 游戏内 `.ac` 控制台指令
│   ├── BotNotificationClient.cs # 向机器人推送 register/unregister/heartbeat/ac
│   ├── Refs/                    # 编译所需 SCPSL 程序集（勿删）
│   └── packages.config          # ExMod.Exiled 9.5.0
│
├── bot/                         # QQ 机器人 + Web 面板（.NET 8）★ 当前活跃目录
│   ├── src/Server_Qcha.Bot/
│   │   ├── Program.cs           # 入口：DI 注册 + Kestrel + 面板挂载
│   │   ├── Bot/
│   │   │   ├── GoCqHttpBotService.cs          # OneBot 11 正向 WS（含断线重连）
│   │   │   ├── CommandRouter.cs               # QQ 群指令路由
│   │   │   ├── CommandParsing.cs              # `#n` / `/ban` 解析
│   │   │   └── BotSessionAccessor.cs          # 会话桥接
│   │   ├── Socket/
│   │   │   ├── ServerRegistry.cs              # 服务器注册表：心跳/清理/排序
│   │   │   ├── SocketCommandClient.cs         # 命令下发客户端
│   │   │   └── BotNotificationListenerService.cs  # 监听 10088
│   │   ├── Data/PlayerRepository.cs           # MySQL 玩家数据（半成品）
│   │   ├── Configuration/                     # Options（含 WebPanelOptions）
│   │   ├── Web/                               # ★ 面板后端（5 个文件）
│   │   ├── wwwroot/                           # ★ 面板前端（index.html + vendor/）
│   │   ├── appsettings.json
│   │   └── Server_Qcha.Bot.csproj             # Microsoft.NET.Sdk.Web
│   └── tests/Server_Qcha.Bot.Tests/            # xUnit（仅 CommandParsing 用例）
│
├── Server_Qcha.Bot/             # ⚠ 旧目录快照，勿修改（见第十节第 1 条）
├── Server_Qcha.sln              # 主解决方案（只引用 bot/ 与 server/）
├── README.md                    # 面向使用者的部署教程
├── RELEASE_NOTES.md             # v1.3.0 版本说明
├── CHANGELOG.md                 # v1.2.0 变更日志
└── 通信协议文档.md               # 完整 TCP 协议规范 + Python/C#/Node 示例
```

---

## 四、核心模块说明

### 4.1 游戏服插件（`server/`）

| 文件 | 职责 | 关键点 |
|------|------|--------|
| `Main.cs` | 插件生命周期 | `OnWaitingForPlayers` 启动 TCP 服务；端口从 `TcpPort` 起最多探测 100 个；启动 `HeartbeatLoop` |
| `TcpCommandServer.cs` | TCP 监听 | 读一次 → 校验 Token（失败回 `Unauthorized`）→ 分发 → 写回，2 秒读超时 |
| `CommandDispatcher.cs` | 指令实现 | 纯字符串匹配；`rest` 有「回合开始 60 秒内」限制 |
| `AcCommand.cs` | `.ac` 指令 | 推送机器人 + 给所有在线管理员屏幕广播 10 秒 |
| `BotNotificationClient.cs` | 主动推送 | **发完必须 `Shutdown(Send)` 半关闭**，再读 `"OK"`；5 秒超时 |

心跳策略：正常 30 秒；失败按 `30 → 60 → 120 → 240 → 300` 秒指数退避，连续 10 次失败进入 300 秒慢速模式。

### 4.2 QQ 机器人（`bot/`）

| 文件 | 职责 | 关键点 |
|------|------|--------|
| `GoCqHttpBotService.cs` | 连接 NapCatQQ | 断线自动重连（5s 起线性退避，30s 封顶）；异常不再冒泡导致进程退出 |
| `CommandRouter.cs` | 群指令路由 | 非管理指令无权限要求；`/bc` `/round` `/ban` 需群管理/群主 |
| `ServerRegistry.cs` | 服务器拓扑 | `{host}:{port}` 复合键；60 秒扫描，90 秒无心跳的动态条目移除；静态条目豁免 |
| `SocketCommandClient.cs` | 下发指令 | 短连接，拼 `Token||命令`，读超时 + 重试 |
| `BotNotificationListenerService.cs` | 接收推送 | 读到流结束（依赖对端半关闭）→ 校验 Token → 按 `type` 路由 → 回 `"OK"` |

**排序规则**（决定群内 `#1` `#2` 与面板索引）：`SortOrder > 0` 的手动优先，其余按 `GamePort` 自动排，再按 host/port 兜底。

### 4.3 Web 面板（`bot/.../Web` + `wwwroot`）

| 文件 | 职责 |
|------|------|
| `PanelDatabase.cs` | SQLite 单文件账号库（`panel.db`），WAL 模式，密码哈希与审计表 |
| `PanelAuthService.cs` | 内置管理员初始化（启动随机重置密码）、PBKDF2-SHA256 哈希、内存会话 |
| `ServerCommandGateway.cs` | **命令网关抽象**，当前实现走插件 TCP，为 LocalAdmin 预留 |
| `PanelEndpoints.cs` | 19 个 HTTP API + 权限校验 + 审计写入 |
| `wwwroot/index.html` | 单页应用（Vue 3 + Element Plus，库文件已本地化，无外网依赖） |

---

## 五、通信协议

完整规范见 **`通信协议文档.md`**，此处仅摘要。

### 5.1 通道一：命令通道（Bot → Server，TCP 10087）

- 短连接：建立 → 发送 → 读一次响应 → 关闭
- 报文：`{AuthToken}||{命令}`（Token 为空则直接发命令）
- 鉴权失败返回字符串 `Unauthorized`

支持的命令：

| 命令 | 参数 | 说明 |
|------|------|------|
| `cx` | — | 在线人数（`在线人数:12/40`） |
| `info` | — | 服务器详情（角色/回合/时间） |
| `list` | — | 玩家列表，格式 `昵称-ID` 每行一个 |
| `start` | — | 强制开始回合 |
| `rest` | — | 重启回合（回合开始 60 秒内才允许） |
| `allrest` | — | 重启整个游戏服进程（危险） |
| `bc&{消息}` | 消息内容 | 全服广播 15 秒 |
| `ychhe&{消息}` | 消息内容 | 自动广播并锁大厅 |
| `kick&{ID}&{原因}&{时间}` | 秒数 | 封禁玩家（注意参数顺序） |
| `ac` | — | 旧版 AC 拉取，**已废弃**（改用通知通道） |

### 5.2 通道二：通知通道（Server → Bot，TCP 10088）

- 短连接 + **半关闭定界**：发完数据必须 `Shutdown(Send)`，对端读到流结束才处理
- 报文：`{AuthToken}||{JSON信封}`，信封形如 `{"type":"...","data":{...}}`
- 成功处理回写两字节 `"OK"`；鉴权失败静默丢弃

四种消息类型：

| type | 用途 | data 关键字段 |
|------|------|--------------|
| `register` | 插件启动注册 | `name` `connectHost` `port` `gamePort` `sortOrder` |
| `unregister` | 插件正常关闭 | `connectHost` `port` |
| `heartbeat` | 每 30 秒保活（兼断线重注册） | 同 `register` |
| `ac` | 推送消息到 QQ 群 | `message` |

---

## 六、配置说明

### 6.1 游戏服插件（EXILED 的 `config.yml`）

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `tcp_port` | `10087` | TCP 命令起始端口，被占用自动递增 |
| `i_p` | `127.0.0.1` | 命令服务监听 IP |
| `server_name` | `1服` | 服务器显示名 |
| `bot_i_p` | `127.0.0.1` | 机器人通知服务 IP |
| `bot_port` | `10088` | 机器人通知服务端口 |
| `auth_token` | `QchaSecret_123` | **须与机器人端一致，生产环境务必修改** |
| `sort_order` | `0` | >0 手动排序，=0 按游戏端口自动排序 |
| `connect_host` | `""` | 机器人回连本机的 IP；同机留空，跨机必须填写 |
| `display_mode` | `2` | `0` 显示查询时间 / `1` 显示 ContentText / `2` 空白 |
| `content_text` | `""` | `display_mode=1` 时显示的内容 |
| `debug` | `false` | 是否输出调试日志 |

### 6.2 QQ 机器人（`appsettings.json`）

```json
{
  "GoCqHttp": {
    "WsBaseUri": "ws://127.0.0.1:6700",
    "ReconnectDelaySeconds": 5,
    "MaxReconnectDelaySeconds": 30
  },
  "SocketServer": {
    "Host": "127.0.0.1",
    "Ports": [ 10087 ],
    "NotificationHost": "0.0.0.0",
    "NotificationPort": 10088,
    "AuthToken": "QchaSecret_123",
    "ConnectTimeoutMs": 10000,
    "ReadTimeoutMs": 2000,
    "Retries": 3,
    "RetryDelayMs": 1000
  },
  "WebPanel": {
    "Enabled": true,
    "Host": "0.0.0.0",
    "Port": 8080,
    "SessionMinutes": 480,
    "DatabasePath": "panel.db",
    "DefaultAdminUsername": "admin",
    "ResetBuiltInPasswordOnStartup": true
  },
  "MySql": { "ConnectionString": "" },
  "Bot": {
    "AllowedGroupIds": [],
    "NotifyGroupIds": [],
    "AcTargetGroupId": 0
  }
}
```

要点：

- `SocketServer:Ports` 只用于**静态兜底**。正常情况下服务器由插件自动注册，留空 `[]` 也能工作。
- `MySql:ConnectionString` 默认为空，**不配置则 `/bd`、`/me` 指令会抛异常**，其余功能不受影响。
- `WebPanel:ResetBuiltInPasswordOnStartup` 为 `true` 时，**每次启动都重置内置管理员密码**并输出到日志。若想固定密码，改为 `false`。
- 配置优先级（后者覆盖前者）：`appsettings.json` → `appsettings.Local.json` → 环境变量（如 `MySql__ConnectionString`）。
- `appsettings.Local.json` 已在 `.gitignore` 中，用于本地私有配置。

---

## 七、部署与运行

### 7.1 环境要求

| 组件 | 要求 |
|------|------|
| SCPSL 服务端 | 已安装 **EXILED 9.5.0** |
| QQ 框架 | **NapCatQQ**（OneBot 11），开启正向 WebSocket 服务端，默认 `127.0.0.1:6700` |
| 机器人 | **.NET 8 SDK / 运行时**（仅编译需要 SDK，运行只需 Runtime） |
| 面板 | 现代浏览器（Chrome / Edge / Firefox） |

### 7.2 启动步骤

1. 安装并登录 NapCatQQ，开启正向 WS（`127.0.0.1:6700`）
2. 将编译好的 `Server_Qcha.dll` 放入 EXILED 插件目录（Windows：`...\EXILED\Plugins\`），重启游戏服生成配置
3. 按 6.1 配置插件，**确保 `auth_token` 与机器人端一致**
4. 复制 `appsettings.Example.json` 为 `appsettings.Local.json`，按 6.2 填写
5. 启动机器人：

```powershell
dotnet run --project bot/src/Server_Qcha.Bot -c Release
```

6. 从启动日志中取面板随机密码，浏览器打开 `http://<服务器IP>:8080/`

### 7.3 端口清单

| 端口 | 协议 | 用途 | 方向 |
|------|------|------|------|
| `10087` | TCP | 命令通道（可自动递增） | Bot → Server |
| `10088` | TCP | 通知通道 | Server → Bot |
| `8080` | TCP | Web 面板 HTTP | 浏览器 → Bot |
| `6700` | TCP | OneBot 11 正向 WebSocket | Bot → NapCatQQ |
| `7777` | UDP | SCPSL 游戏端口（主机侧） | 玩家 → 游戏服 |

> **安全提示**：面板默认监听 `0.0.0.0:8080` 且为明文 HTTP。公网部署请改为 `127.0.0.1` 并由 Nginx 反代加 HTTPS，或至少用防火墙限制来源 IP。

---

## 八、Web 面板权限模型

权限以位标志存储（`long`），可自由组合。共 8 个权限位：

| Key | 名称 | 说明 |
|-----|------|------|
| `servers.view` | 查看服务器 | 服务器列表、玩家列表、详情 |
| `broadcast.send` | 发送广播 | 全服广播 |
| `players.kick` | 踢出玩家 | 踢出（服务端实现为短时封禁） |
| `players.ban` | 封禁玩家 | 封禁（可设时长与原因） |
| `round.control` | 回合控制 | 开始回合、重启回合 |
| `server.control` | 服务器控制 | 进程级启停（**当前未实现，为 LocalAdmin 预留**） |
| `accounts.manage` | 账号管理 | 增删改账号与其权限 |
| `audit.view` | 审计日志 | 查看操作记录 |

四个预设组合：`只读`（Viewer）、`运营`（Operator）、`管理员`（Admin）、`所有者`（Owner，全部权限）。

**安全保护**

- 内置管理员账号 `admin` **不可禁用、不可删除**（防止把自己锁在门外）
- 不能禁用或删除当前登录的账号
- 密码使用 PBKDF2-SHA256 + 随机盐（10 万次迭代），不可逆
- 会话存内存，重启失效；账号被禁用/删除时其在线会话立即失效
- 所有写操作（广播、踢人、封禁、账号变更等）写入审计日志

---

## 九、完成度与 TODO

### 已完成

- [x] 「QQ 群 → 游戏服」命令通道（含 Token 鉴权）
- [x] 「游戏服 → QQ 群」通知通道（JSON 信封协议）
- [x] 服务器自动端口探测 + 动态注册 + 心跳保活 + 指数退避
- [x] 三层离线感知（正常注销 0 秒 / 指令失败 <1 秒 / 心跳超时 ≤90 秒）
- [x] 多服多级排序（`sort_order` + `game_port`）
- [x] 游戏内 `.ac` 消息推送到 QQ 群 + 管理员屏幕广播
- [x] 机器人 WebSocket 断线自动重连
- [x] **Web 管理面板**：登录、账号管理、权限分配、服务器/玩家查看、广播/踢出/封禁/回合控制、审计日志
- [x] 面板前后端离线可用（前端库已本地化）

### 未完成 / 半成品

- [ ] **数据库查询功能（`PlayerRepository`）** —— 只实现了 `/bd` 绑定与 `/me` 查询两个方法。
      **没有建表脚本**（依赖外部已有的 `playerdata` 表）；`MySql:ConnectionString` 默认空；
      README 的 `[ ] 完善数据库查询功能` 至今未勾选。
- [ ] **`/setadmin` 指令** —— 目前实际下发的是 `bc&{id}&{group}`，并未真正写入权限系统。
- [ ] **`server.control` 权限** —— 权限位已定义，但服务端无对应指令，界面也未暴露相关操作。
- [ ] 排行榜 / 按昵称或 SteamID 反查 / 修改玩家备注等查询场景。
- [ ] 面板缺少实时控制台输出流、定时任务、告警通知等进阶功能。
- [ ] 测试覆盖极低：仅 `CommandParsingTests` 3 个用例，`CommandRouter`、`ServerRegistry`、面板全线无测试。

---

## 十、已知问题与技术债

按处理优先级排序：

### 1. ⚠ 仓库内存在两套 bot 源码，`Server_Qcha.Bot/` 是废弃快照

根目录下同时存在 `bot/` 与 `Server_Qcha.Bot/`，两者源码重复。**`bot/` 是当前活跃目录**，判定依据：

- 主解决方案 `Server_Qcha.sln` 只引用 `bot/src/Server_Qcha.Bot/Server_Qcha.Bot.csproj`
- `bot/` 独有 `Socket/ServerRegistry.cs`、`Socket/BotNotificationListenerService.cs`、`Web/`、`wwwroot/`
- `Server_Qcha.Bot/` 停留在 v1.1.x 能力（无 Token 鉴权、无通知通道、无 `.ac` 推送、无面板）

**结论：所有修改只针对 `bot/`，`Server_Qcha.Bot/` 可以安全删除。** 未删除的原因是避免在同一次变更中混入大范围文件移动，建议后续单独提交一次清理。

### 2. ⚠ 依赖的 EXILED 框架正在被官方 LabAPI 取代

当前 `server/packages.config` 锁定 `ExMod.Exiled 9.5.0`。社区已在推广迁移到官方 **LabAPI**（`northwood-studios/LabAPI`）。若 EXILED 停止跟进游戏版本，本插件会随游戏更新失效。**这是本项目最大的中长期风险。**

### 3. 通知通道依赖 TCP 半关闭来界定消息边界

`BotNotificationListenerService` 通过「读到流结束（FIN）」判断一条消息接收完毕，因此发送端必须 `Shutdown(Send)`。这是脆弱设计：任何一端忘记半关闭，或中间设备缓冲行为异常，都会导致消息处理卡住。

**改进方向**：改为「4 字节长度前缀 + payload」定界，可彻底移除对半关闭的依赖。

### 4. 「踢出」与「封禁」在服务端是同一个指令

服务端只有 `kick` 指令，其实现是 `player.Ban(duration, reason)`。因此面板的「踢出」本质上也是短时封禁（默认 300 秒）。界面上已作提示，但这与用户直觉不完全一致。

### 5. 服务器索引不稳定

群内 `#1` `#2` 与面板 URL 中的 `{index}` 都基于 `ServerRegistry.GetSorted()` 的**当前排序位置**。当某台服务器上下线或 `sort_order` 变化时，索引会整体漂移。更稳妥的做法是改用 `host:port` 作为稳定标识。

### 6. 面板为明文 HTTP 且无防爆破

默认监听 `0.0.0.0:8080`，无 HTTPS、无登录失败次数限制。适用于内网，**不建议直接暴露公网**。

### 7. `AcCommand` 中残留的旧版 AC 拉取逻辑

`Main.SetAcPayload` / `ConsumeAcPayload` 与 `CommandDispatcher.HandleAc` 是为旧版「`ac` 命令拉取」保留的，现已改用通知通道推送，该路径**实际不再被使用**，属可清理的冗余代码。

### 8. MySQL 相关功能依赖外部表结构

`PlayerRepository` 查询的表 `playerdata`（字段 `Id` `PlayerName` `ScpsKilled` `PlayersKilled` `PlayTime` `Deaths` `Admin` `IsAdmin` `QQ_ID`）由项目外部的统计插件创建，本项目只读写、不建表。

---

## 十一、构建与开发

### 11.1 编译

**Bot 端（含 Web 面板，.NET 8）**

```powershell
# 构建
dotnet build bot/src/Server_Qcha.Bot/Server_Qcha.Bot.csproj -c Release

# 运行
dotnet run --project bot/src/Server_Qcha.Bot -c Release

# 测试（目前只有指令解析用例）
dotnet test bot/tests/Server_Qcha.Bot.Tests/Server_Qcha.Bot.Tests.csproj
```

**Server 端（.NET Framework 4.8.1，旧式 csproj）**

需使用 Visual Studio 或 MSBuild（`dotnet build` 不支持旧式 csproj）：

```powershell
msbuild server/Server_Qcha.csproj /p:Configuration=Release
```

编译依赖 `server/Refs/` 下的 SCPSL 程序集与 NuGet 包 `ExMod.Exiled 9.5.0`。

### 11.2 开发注意事项

- **Bot 端已改为 `Microsoft.NET.Sdk.Web`**（为承载 Kestrel）。`Program.cs` 使用 `WebApplication.CreateBuilder` 而非 `Host.CreateApplicationBuilder`。
- **前端不需要 Node 构建链**。`wwwroot/index.html` 是单文件 SPA，Vue 3 与 Element Plus 以本地 `vendor/` 文件引入，改完直接刷新页面即可。若要升级前端库，替换 `wwwroot/vendor/` 下对应文件并同步 `index.html` 中的版本引用。
- **模板中禁止使用自闭合的自定义组件标签**。`<el-input ... />` 这类写法在原生 HTML 解析下**无效**，会导致后续兄弟元素被错误嵌套。必须写成 `<el-input ...></el-input>`。这个坑曾导致服务器列表整块不渲染。
- **Vue `setup()` 中模板用到的所有变量都必须出现在 `return {}` 里**，漏一个会导致该区域渲染异常。
- `panel.db` 含密码哈希，已加入 `.gitignore`，**切勿提交**。

---

## 十二、后续演进建议

按优先级排序：

### 高优先级

1. **评估并迁移到 LabAPI**（技术债第 2 条）。这是决定项目寿命的事，越早越好。
2. **清理 `Server_Qcha.Bot/` 旧目录**，消除「改错文件」的隐患。
3. **补完数据库功能**：提供建表脚本、明确 `playerdata` 的来源与初始化方式，或改为项目自建表。

### 中优先级

4. **实现 `LocalAdminCommandGateway`**：`IServerCommandGateway` 接口已就绪，只需新增一个实现类并在 `Program.cs` 替换注册，即可让面板具备进程级启停能力（对应 `server.control` 权限）。这一步能让面板真正「可替代 LocalAdmin」。
5. **协议加长度前缀**，移除对 TCP 半关闭的依赖。
6. **面板增强**：SignalR 实时控制台输出流、玩家列表自动刷新、定时任务（定时广播/重启）、异常告警（可直接复用 QQ 群通知通道）。

### 低优先级

7. 补充测试：优先覆盖 `CommandParsing`、`ServerRegistry` 排序与清理、`PanelAuthService` 密码与会话。
8. 服务器标识由索引改为 `host:port`。
9. 面板增加登录失败限流与 HTTPS 支持。

---

## 附录：快速排错

| 现象 | 排查方向 |
|------|---------|
| 机器人在启动后立刻退出 | 早期版本问题，现已有自动重连；确认是否为最新代码 |
| 日志反复 `连接 go-cqhttp 失败` | NapCatQQ 未启动，或未开启正向 WS，或端口不是 `6700` |
| 群指令无响应 | 插件是否加载；`tcp_port` 与机器人配置是否对应 |
| 指令返回 `Unauthorized` | 双端 `auth_token` 不一致 |
| `.ac` 推送未到群 | `Bot:AcTargetGroupId` 是否配置；插件 `bot_i_p`/`bot_port` 是否可达；防火墙是否放行 `10088` |
| 面板打不开 | 进程是否启动；`WebPanel:Enabled` 是否为 `true`；`8080` 是否被占用或未放行 |
| 面板登录失败 | 密码在**当前这次**启动日志中，每次启动都会变化 |
| 面板操作返回「服务器不在线」 | 游戏服插件未注册或已掉线；查看机器人日志中的注册表状态 |
| `/bd`、`/me` 报错 | `MySql:ConnectionString` 未配置 |
| 服务器列表页空白（无卡片） | 前端模板若被改回自闭合标签写法会导致此问题，参考第十一节说明 |

---

*本文档依据 2026-09-20 的工作区代码编写。若代码有较大变更，请同步更新本文档。*


# 机器人接入模式说明（NapCat / QQ 官方 Bot API）

本文档说明 Qcha 机器人同时支持的两套接入方式、如何在运行时切换，以及 QQ 官方平台的能力边界。
实现位于 `bot/src/Server_Qcha.Bot/Bot/`。

---

## 一、两种模式对比

| 维度 | NapCat（OneBot 11） | QQ 官方 Bot API v2 |
|---|---|---|
| 连接方向 | 机器人**主动连**本机 NapCat 正向 WS | 机器人**主动连**开放平台网关（需外网） |
| 鉴权 | 无 / 本地 token | AppID + AppSecret 换 AccessToken，头 `Authorization: QQBot {token}` |
| 群标识 | 真实群号（数字） | `group_openid`（字符串） |
| 用户标识 | 真实 QQ 号（数字） | `member_openid` / `user_openid`（字符串） |
| 群消息可见性 | 全部群消息 | **仅 @机器人 的消息**，且 @前缀已由平台剥离 |
| 群列表 / 群名 | 可获取 | **无接口**，只能记录「曾被 @ 过的群」 |
| 好友列表 / 用户昵称 | 可获取 | **无接口**，只有 OpenID |
| 消息格式 | 支持 CQ 码富文本 | 纯文本 + 媒体，**不支持 CQ 码** |
| 消息长度 | 约 4000 字符 | 有上限，超长需分段（本项目默认 800，可配） |
| 主动推送 | 无限制 | **有配额限制**，默认关闭 |
| 回复方式 | 直接发送 | 带 `msg_id` + 递增 `msg_seq` 才算被动回复，同一消息最多 5 次 |
| 权限判定 | 群角色 `role` = admin/owner | 群角色 `member_role` = admin/owner，可另配 OpenID 白名单兜底 |

> 选择建议：需要**完整群管理能力**（群列表、全量消息、私聊任意用户）用 NapCat；
> 需要**合规、稳定、免自建客户端**用官方 API，但要接受上表的能力削减。

---

## 二、如何切换

### 方式一：Web 面板（推荐，热切换）

「QQ 机器人」页 → **一、接入模式** → 选择后点「保存设置」。

保存后由 `BotHostService` 监听配置热重载，自动停掉旧连接、建立新连接，**无需重启进程**。
同一时刻只有一套通道工作，不会出现两条通道重复回复同一条指令。

### 方式二：配置文件

`bot-settings.json`（面板可写文件，运行时优先于 `appsettings.json`）：

```json
{
  "Bot": { "Mode": "OfficialQq" }
}
```

`Mode` 取值：`NapCat`（默认）或 `OfficialQq`。

---

## 三、官方模式配置项

面板对应「二、QQ 开放平台凭证」与「三、官方平台权限与目标」，配置键为 `OfficialQq` 段。

| 配置项 | 说明 |
|---|---|
| `ApiBase` | 开放平台 API 根地址，默认 `https://api.bot.qq.com`（旧域名 `https://api.sgroup.qq.com`） |
| `AppId` / `ClientSecret` | 开放平台「开发设置」获取。**ClientSecret 仅创建时展示一次**，面板不回显，留空表示不修改 |
| `Sandbox` | 沙箱环境，开启后主机名自动加 `sandbox.` 前缀 |
| `Intents` | 事件订阅位，默认 `33554432`（`1<<25`，群聊 @ 与单聊消息） |
| `MaxTextLength` | 单条文本上限，超长自动分段并递增 `msg_seq` |
| `AllowActivePush` | 是否允许主动推送。关闭时仅回复用户消息，`.ac` 游戏推送会被跳过并记警告 |
| `AdminOpenIds` | 管理员 OpenID 白名单，作为 `member_role` 的兜底；**单聊场景必须在此登记**才能用管理指令 |
| `AllowedGroupOpenIds` | 群消息白名单，留空不限 |
| `NotifyGroupOpenIds` / `AcTargetGroupOpenId` | 运营通知群 / 游戏 `.ac` 推送目标群 |
| `NotifyPrivateOpenIds` | 运营私聊通知目标 |

官方平台拿不到群列表，面板「群列表」区域在官方模式下展示的是**机器人曾被 @ 过的群**，
其 OpenID 可直接点「+ 白名单 / 设为通知群」写入配置。

### 在面板上怎么操作

「QQ 机器人」页从上到下：

1. **一、接入模式** —— 下拉选择 `NapCat / OneBot 11` 或 `QQ 官方 Bot API`；
   右侧「指令清单」按钮导出官方管理端需要登记的指令表（可直接照抄）。
2. **二、QQ 开放平台凭证**（仅官方模式显示）—— AppID、AppSecret、API 根地址、沙箱开关，
   以及**「测试凭证」按钮**：只申请一次 AccessToken 来验证凭证是否正确，
   **不切换模式、不保存配置**，AppSecret 留空则用已保存的那个。
   官方在凭证有误时同样返回 HTTP 200，面板会展示平台给的原始错误码
   （例如 `10004 机器人不存在` 说明 AppID 填错了）。
3. **三、官方平台权限与目标** —— 管理员 OpenID、群白名单、通知群、`.ac` 推送目标群。
4. **保存设置** —— 写入 `bot-settings.json`，随即触发热重载完成模式切换。

> AppSecret 按设计**不回显**（只在保存时写入配置文件），所以再次打开页面时输入框显示
> 「已保存（留空表示不修改）」；不填就是不修改，不会把它清空。
>
> 保存后界面会短暂显示目标模式再回读真实状态（配置热重载是异步的，前端做了收敛轮询），
> 因此**不会出现"刚切完又被弹回去"的假象**。

---

## 四、官方接入协议要点（实现依据）

```
1) POST {ApiBase}/app/getAppAccessToken   body: {"appId","clientSecret"}
   → {"access_token":"...","expires_in":"7200"}        # expires_in 可能是字符串或数字
2) GET  {ApiBase}/gateway   header: Authorization: QQBot {access_token}
   → {"url":"wss://api.sgroup.qq.com/websocket/"}
3) 建立 WS 长连接，收到 op 10 Hello（含 heartbeat_interval）
4) 发 op 2 Identify：{token:"QQBot {token}", intents, shard:[0,1], properties:{...}}
   断线重连改用 op 6 Resume（session_id + 最后一次 seq）
5) 按间隔发 op 1 Heartbeat（d = 最近收到的 s），收到 op 11 表示成功
```

`opcode`：`0` Dispatch（`t` 事件名 / `d` 数据 / `s` 序号）、`1` 心跳、`2` 鉴权、`6` 恢复、
`7` 服务端要求重连、`9` 会话失效、`10` Hello、`11` 心跳 ACK。

订阅事件：`GROUP_AT_MESSAGE_CREATE`（群 @）、`C2C_MESSAGE_CREATE`（单聊），
`READY` / `RESUMED` 用于建立与恢复会话。

发送消息：

```
POST /v2/groups/{group_openid}/messages        # 群
POST /v2/users/{user_openid}/messages          # 单聊
body: {"content":"...","msg_type":0,"msg_id":"...","msg_seq":1}
```

`msg_seq` 在同一 `msg_id` 下递增且不可重复（重复会被平台去重丢弃），最多 5 次。

---

## 五、指令清单

指令目录是全项目唯一事实来源，定义在 `Bot/BotCommandCatalog.cs`，
可通过 `GET /api/bot/commands` 或面板「查看官方管理端指令配置清单」导出。

| 指令 | 别名 | 用法 | 说明 | 权限 |
|---|---|---|---|---|
| `help` | `h` `menu` | `/help` | 查看全部可用指令 | 所有人 |
| `cx` | — | `/cx` | 查询所有在线服务器的人数 | 所有人 |
| `info` | — | `/info` | 查询所有服务器运行信息 | 所有人 |
| `list` | — | `/list <服务器序号>` | 查看指定服务器玩家列表 | 所有人 |
| `bind` | `bd` | `/bd <Steam64>` | 绑定你的 Steam64 账号 | 所有人 |
| `me` | `stat` | `/me` | 查询自己绑定的玩家数据 | 所有人 |
| `version` | `qcha` `ver` | `/version` | 查看机器人版本信息 | 所有人 |
| `broadcast` | `bc` | `/bc <序号> <内容>` | 向指定服务器广播消息 | 管理员 |
| `round` | — | `/round <序号>` | 重启指定服务器回合 | 管理员 |
| `ban` | `kick` | `/ban <序号> <ID> <时间> <原因>` | 踢出或封禁玩家 | 管理员 |
| `setadmin` | — | `/setadmin <序号> <ID> <分组>` | 设置玩家权限组 | 管理员 |

### 官方平台管理端需要登记的指令

QQ 开放平台要求指令在管理端配置（**上限 24 条**，指令名 ≤8 个中文字符或 16 个英文字符，
介绍 ≤15 个中文字符或 30 个英文字符）。可直接照抄下表：

| 指令名 | 指令介绍 | 权限菜单建议 | 使用场景 |
|---|---|---|---|
| `/help` | 查看全部可用指令 | 所有用户 | QQ 群、消息列表 |
| `/cx` | 查询全服在线人数 | 所有用户 | QQ 群、消息列表 |
| `/info` | 查询服务器运行信息 | 所有用户 | QQ 群、消息列表 |
| `/list` | 查看服务器玩家列表 | 所有用户 | QQ 群、消息列表 |
| `/bd` | 绑定你的 Steam64 账号 | 所有用户 | QQ 群、消息列表 |
| `/me` | 查询自己的玩家数据 | 所有用户 | QQ 群、消息列表 |
| `/version` | 查看机器人版本信息 | 所有用户 | QQ 群、消息列表 |
| `/bc` | 向服务器广播消息 | 仅频道主和管理员 | QQ 群 |
| `/round` | 重启服务器回合 | 仅频道主和管理员 | QQ 群 |
| `/ban` | 踢出或封禁玩家 | 仅频道主和管理员 | QQ 群 |
| `/setadmin` | 设置玩家权限组 | 仅频道主和管理员 | QQ 群 |

### 触发方式（官方规范）

- **群聊**：必须 `@机器人` 后接指令，或直接发送 `/指令`（避免误触发）。
- **私聊**：直接发送指令即可，`/` 前缀可选。
- 指令与参数**必须用空格分隔**（`/抽卡 一` 而非 `/抽卡一`）。
- 解析器同时兼容全角斜杠 `／`、全角井号 `＃` 与全角空格，并会剔除零宽字符。
- 兼容用户既有习惯的无前缀写法（`cx`、`info`、`help`）与 `#qcha`、`#1` 旧语法。
- 只有带 `/` 或 `#` 前缀的未知指令才会回提示；群里正常聊天不会被误回。

---

## 六、代码结构

新增（`Bot/` 目录）：

| 文件 | 职责 |
|---|---|
| `BotAbstractions.cs` | 平台枚举、统一的入站消息 / 发送结果、`IBotClient` 接口、`BotClientAccessor` |
| `BotClientHost.cs` | 可反复启停的接入宿主基类（`BackgroundService` 的停止令牌是一次性的，不满足热切换） |
| `NapCatBotClient.cs` | NapCat / OneBot 11 实现（由原 `GoCqHttpBotService` 重构） |
| `OfficialQqBotClient.cs` | 官方网关连接、心跳、Resume、事件分发、REST 发送与分段 |
| `OfficialQqTokenProvider.cs` | AccessToken 申请与提前 60 秒续期（并发安全） |
| `BotHostService.cs` | 按 `Bot.Mode` 启停对应实现，监听配置热重载完成切换 |
| `BotCommandCatalog.cs` | 指令目录（帮助文本 + 官方管理端清单的唯一来源） |
| `BotCommandParser.cs` | 指令归一化与解析、按平台裁剪/分段文本 |
| `BotBindingStore.cs` | OpenID → Steam64 绑定表（SQLite） |

改动：

- `CommandRouter` 改为接收 `IBotClient` + `BotIncomingMessage`，不再依赖 `CqWsSession`。
- `BotNotificationListenerService`、`PanelEndpoints`、`BotEndpoints` 改用 `IBotClient` / `BotClientAccessor`。
- `PlayerRepository` 增加 `GetByPlayerIdAsync` / `PlayerExistsAsync`（官方模式经绑定表解析后按 Steam64 查询）。
- 已删除 `GoCqHttpBotService.cs`、`BotSessionAccessor.cs`（内容分别并入 `NapCatBotClient.cs`、`BotAbstractions.cs`）。

---

## 七、绑定机制的差异

`/bd <Steam64>` 在两个模式下写入的位置不同：

- **NapCat**：QQ 号是真实数字，直接写回玩家库 `playerdata.QQ_ID` 列（与历史行为一致）。
- **官方平台**：只有 OpenID（字符串），无法写入整型的 `QQ_ID` 列，
  因此写入独立表 `bot_bindings(platform, external_id, player_id)`（位于面板 SQLite 库 `panel.db`）。
  换 QQ 账号后 OpenID 变化，需要重新绑定。

---

## 八、排错

| 症状 | 原因与处理 |
|---|---|
| 官方模式日志反复出现「缺少 AppId / ClientSecret」 | 尚未填写凭证，或在面板保存时 AppSecret 留空且此前从未保存过 |
| 切换模式后无反应 | 面板保存返回的 `modeChanged` 为 true 时由配置热重载驱动切换；若 `bot-settings.json` 被手工改坏（JSON 语法错），配置源会重载失败，看启动日志有无配置异常 |
| 群里 @机器人 没反应 | 官方平台只推送 @机器人 的消息；另外检查 `AllowedGroupOpenIds` 白名单与该群是否在沙箱白名单内 |
| 管理指令提示权限不足 | 官方群使用 `member_role` 判定；若平台未下发角色，需把该用户 OpenID 加入 `AdminOpenIds` |
| `.ac` 推送不出去 | 官方模式默认关闭主动推送，需开启 `AllowActivePush`（注意配额限制） |
| 面板所有接口 500 | 检查是否有服务只通过 `AddHostedService<T>()` 注册却被 Minimal API 以具体类型注入——`AddHostedService<T>` 不注册 `T` 本身，会导致参数被当作请求体推断 |
